#!/bin/bash

if pgrep -x hypridle >/dev/null; then
  pkill -x hypridle
  notify-send "󱌙    Caffeinated! I won't fall asleep."
else
  uwsm-app -- hypridle >/dev/null 2>&1 &
  sleep 0.5
  notify-send "󱌜    Decaffeinated! I will fall asleep when idle."
fi

pkill -RTMIN+9 waybar
